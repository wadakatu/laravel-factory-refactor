```astro
---
interface FeatureCardProps {
  icon: string;
  title: string;
  description: string;
  onHover: () => void;
}

export const FeatureCard = ({
  icon,
  title,
  description,
  onHover,
}: FeatureCardProps) => (
  <div class="feature-card" onMouseEnter={onHover}>
    <img src={icon} alt={title} />
    <h2>{title}</h2>
    <p>{description}</p>
  </div>
);
---

<style>
  :root {
    --primary-color: #0a74da;
    --secondary-color: #f5b700;
    --accent-color: #0f4c81;
    --background-color: #ffffff;
    --surface-color: #f8f9fa;
    --text-primary-color: #333333;
    --text-secondary-color: #555555;
    --text-accent-color: #0a74da;
    --font-heading: 'Roboto', sans-serif;
    --font-body: 'Open Sans', sans-serif;
    --font-code: 'Fira Code', monospace;
    --body-font-size: 16px;
    --body-line-height: 1.5;
    --border-radius: 8px;
    --transition-duration: 0.3s;
  }

  .feature-card {
    border: 1px solid var(--surface-color);
    border-radius: var(--border-radius);
    padding: 16px;
    text-align: center;
    transition: transform var(--transition-duration);
    background-color: var(--background-color);
    color: var(--text-primary-color);
    font-family: var(--font-body);
    font-size: var(--body-font-size);
    line-height: var(--body-line-height);
  }

  .feature-card:hover {
    transform: scale(1.05);
  }

  .feature-card img {
    max-width: 100%;
    height: auto;
    border-radius: var(--border-radius);
  }

  .feature-card h2 {
    font-family: var(--font-heading);
    font-weight: 700;
    color: var(--text-accent-color);
  }

  .feature-card p {
    color: var(--text-secondary-color);
  }

  @media (max-width: 768px) {
    .feature-card {
      width: 100%;
    }
  }
</style>
```

This Astro component file includes TypeScript type definitions, uses CSS custom properties for theming, and ensures accessibility and performance considerations. It leverages modern CSS techniques like transitions and responsive design.