```astro
---
// Hero.astro
const { stars, forks } = { stars: 1, forks: 0 };
---

<section class="hero" style="background-color: var(--background); padding: 2rem 0;">
  <div class="container">
    <div class="grid">
      <div class="hero-content">
        <h1 class="hero-title" style="color: var(--primary); animation: fadeIn 1s;">
          laravel-factory-refactor
        </h1>
        <p class="hero-description" style="color: var(--secondary); animation: fadeIn 1.5s;">
          Refactor the style of factory call from helper to static in Laravel 8.x, 9.x, 10.x.
        </p>
        <div class="stats" style="animation: fadeIn 2s;">
          <span class="stat" style="color: var(--accent);">
            ⭐ {stars} Stars
          </span>
          <span class="stat" style="color: var(--accent);">
            🍴 {forks} Forks
          </span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    text-align: center;
  }
  .grid {
    display: grid;
    place-items: center;
  }
  .hero-title {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
  }
  .hero-description {
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }
  .stats {
    font-size: 1rem;
  }
  .stat {
    margin: 0 1rem;
  }
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
```