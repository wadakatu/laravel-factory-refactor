```astro
---
interface Props {
  submitButtonLabel: string;
}

export const ContactForm = ({ submitButtonLabel }: Props) => {
  return (
    <>
      <form class="contact-form" aria-labelledby="formTitle">
        <h2 id="formTitle" class="visually-hidden">Contact Form</h2>
        <label for="name">名前</label>
        <input type="text" id="name" name="name" required aria-required="true" aria-label="名前" />
        
        <label for="email">メールアドレス</label>
        <input type="email" id="email" name="email" required aria-required="true" aria-label="メールアドレス" />
        
        <label for="message">メッセージ</label>
        <textarea id="message" name="message" required aria-required="true" aria-label="メッセージ"></textarea>
        
        <button type="submit" aria-label={submitButtonLabel}>{submitButtonLabel}</button>
      </form>
    </>
  );
};
---

<style>
  :root {
    --primary-color: #0A74DA;
    --secondary-color: #F5B700;
    --background-color: #FFFFFF;
    --text-primary: #333333;
    --border-color: #ccc;
    --font-heading: 'Roboto', sans-serif;
    --font-body: 'Open Sans', sans-serif;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    max-width: 600px;
    margin: auto;
    background-color: var(--background-color);
    font-family: var(--font-body);
  }

  .contact-form label {
    margin-bottom: 5px;
    color: var(--text-primary);
  }

  .contact-form input,
  .contact-form textarea {
    margin-bottom: 15px;
    padding: 10px;
    border: 1px solid var(--border-color);
    border-radius: 4px;
    font-family: var(--font-body);
    width: 100%;
  }

  .contact-form button {
    padding: 10px 15px;
    background-color: var(--primary-color);
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-family: var(--font-body);
  }

  .contact-form button:disabled {
    background-color: var(--border-color);
    cursor: not-allowed;
  }

  @media (max-width: 600px) {
    .contact-form {
      padding: 10px;
    }
  }

  .visually-hidden {
    position: absolute;
    width: 1px;
    height: 1px;
    margin: -1px;
    padding: 0;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    border: 0;
  }
</style>
```

This Astro component file defines a `ContactForm` component using TypeScript with specified props. The form is styled with custom properties (CSS variables) and modern CSS techniques like Flexbox. Accessibility features are included, such as the use of `aria-label` and a hidden heading for screen readers. The styling adapts for smaller screens using media queries, ensuring a responsive design.