```astro
---
import Layout from '../components/Layout.astro';
import Hero from '../components/Hero.astro';
import Features from '../components/Features.astro';

const readmeContent = `
# Laravel Factory Refactor

This project aims to refactor the style of factory calls from helper to static in Laravel 8.x, 9.x, and 10.x. 

## Installation

To install the package, use Composer:

\`\`\`bash
composer require your-vendor/laravel-factory-refactor
\`\`\`

## Usage

Once installed, you can start using the static factory calls in your models.
`;
---

<Layout>
  <Hero 
    title="Laravel Factory Refactor"
    description="Refactor the style of factory call from helper to static in Laravel 8.x, 9.x, 10.x."
    stars={1}
    forks={0}
  />
  
  <section class="py-5">
    <div class="container">
      <h2 class="text-primary">About the Project</h2>
      <p>{readmeContent}</p>
    </div>
  </section>

  <Features />
</Layout>
```

このコードは、指定された要件に基づいてAstroページを生成します。`Layout`、`Hero`、`Features`コンポーネントを組み合わせ、READMEの一部を表示するセクションを含んでいます。